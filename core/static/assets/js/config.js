var primary = localStorage.getItem("primary") || '#35bfbf';
var secondary = localStorage.getItem("secondary") || '#FF6150';

window.CionAdminConfig = {
	// Theme Primary Color
	primary: primary,
	// theme secondary color
	secondary: secondary,
};
